# Package Modules

Reusable Terraform modules used by the FSS stack package.

- `fss_filesystem/`: filesystem wrapper.
- `fss_mount_target/`: mount target wrapper.
- `fss_export/`: export wrapper.

Start with `../examples/` for runnable root modules.

