# Sprint 8 Generated Terraform

Sprint 8 integration tests generate executable Terraform root modules under this directory:

- `it1_logging_enabled/main.tf`

The generated Terraform is intentionally kept in the sprint directory for operator review. Test-only runtime files such as state, plans, provider caches, and `tf_test_artifacts/` are ignored by `.gitignore`.
